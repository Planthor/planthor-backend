version: '3.8'
name: planthor-webapi-ws

services:
  planthorwebapi:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ..:/workspace/planthorwebapi:cached
    command: sleep infinity
    user: vscode
    networks:
      - app_network

networks:
  app_network:
    driver: bridge
